# Project Radar v3 readiness

The implementation is ready for integration review when its feature workflows, complete test suite, real-snapshot offline build, and publication integrity checks pass. Production readiness additionally requires the main-branch workflow to generate and commit a valid v3 status and manifest without changing v2 production rankings.
