# Project Radar v3 merge gate

Merge requires successful feature workflows, successful pull-request CI across supported Python versions, a clean diff, and confirmation that the additive layer does not mutate v2 production ranking. Main-branch production deployment is accepted only after the v3 workflow commits a valid status and SHA-256 manifest.
